kind: Service
apiVersion: v1
metadata:
  name: vote
  labels:
    app: vote
spec:
  selector:
    app: vote
  type: NodePort
  ports:
  - name: vote-service
    port: 8080
    targetPort:  80
    nodePort: 31000